HEADER COMÚN DE PLANETPEDIA
===========================

header.html  -> contiene el único header común.
header.js    -> lo carga en cada página y corrige automáticamente las rutas.

Páginas actualizadas: 54.

Nota: fetch() debe probarse desde un servidor web (como GitHub Pages). Al abrir los HTML directamente con file:// algunos navegadores bloquean fetch().
