tippy('.especie-link', {
        allowHTML: true,       // Permite meter código HTML (imágenes y párrafos) dentro del popup
        theme: 'wiki',         // Usa el diseño personalizado que creamos arriba
        animation: 'scale',    // Efecto de aparición suave
        placement: 'top',      // Posición por defecto (arriba del enlace)
        interactive: true      // Permite al usuario mover el ratón dentro del popup si lo necesita
    });